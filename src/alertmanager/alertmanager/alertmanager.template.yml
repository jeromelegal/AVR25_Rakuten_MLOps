global:
  resolve_timeout: 1m
  slack_api_url: SLACK_API_URL

route:
  receiver: 'slack-notifications'

receivers:
- name: 'slack-notifications'
  slack_configs:
  - channel: '#avr25-mle-rakuten-alert'
    send_resolved: true
